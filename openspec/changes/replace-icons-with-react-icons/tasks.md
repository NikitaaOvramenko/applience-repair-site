## 1. Setup

- [x] 1.1 Install `react-icons`.

## 2. Component Implementation

- [x] 2.1 Update `ServiceList.tsx`:
  - Replace emojis with icons (e.g., `FaTools`, `TbFridge`, `MdLocalLaundryService`, `GiGasStove`, etc.).
- [x] 2.2 Update `ServiceSelection.tsx`:
  - Sync icons with `ServiceList` for consistency.
- [x] 2.3 Update `ContactFooter.tsx`:
  - Replace "Time", "Trophy", "Scroll", "Shield" emojis with `FaClock`, `FaAward`, `FaCertificate`, `FaShieldAlt`.
  - Replace Contact button emojis with `FaPhone`, `FaWhatsapp`, `FaComment`.

## 3. Verification

- [x] 3.1 Verify build (`npm run build`).
- [x] 3.2 Visual check (ensure icons render correctly and sizing is appropriate).
