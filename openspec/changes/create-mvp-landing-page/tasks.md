## 1. Setup & Configuration

- [x] 1.1 Configure Tailwind CSS with project colors (Primary Blue `#005BBB`) and fonts (Montserrat/Roboto/Inter).
- [x] 1.2 Establish project folder structure: `src/components`, `src/pages`, `src/assets`, `src/styles`.
- [x] 1.3 Add localization support structure (ensure all text is English).

## 2. Component Implementation

- [x] 2.1 Create `Hero` component with background image, headline, and "Call Now" CTA.
- [x] 2.2 Create `ServiceList` component with icons/text for each appliance type.
- [x] 2.3 Create `ServiceAreas` component listing GTA locations.
- [x] 2.4 Create `ContactFooter` component with WhatsApp/SMS/Call buttons.
- [x] 2.5 Implement `Layout` component for shared structure.

## 3. Page Assembly

- [x] 3.1 Build `HomePage` using implemented components.
- [x] 3.2 (Optional) Build `ServicesPage` for detailed listings if needed (or keep as single page section).
- [x] 3.3 Implement smooth scroll behavior for anchor links.

## 4. Performance & Assets

- [x] 4.1 Compress and optimize all images to < 250KB.
- [x] 4.2 Verify mobile responsiveness (Call buttons functional).
- [x] 4.3 Verify load time < 2s.

## 5. Deployment Prep

- [x] 5.1 Configure build output for target hosting (Netlify/Vercel compatible).
- [x] 5.2 Verify SEO meta tags (Title, Description).
