## 1. Project Setup
- [ ] 1.1 Create `TCS-Junk-Removal` directory and initialize Vite React TS project.
- [ ] 1.2 Install dependencies: `tailwindcss`, `@tailwindcss/vite`, `react-router-dom`, `react-icons`, `axios`, `three` (if needed, or omit if not using 3D), `gh-pages`, `uuid`, `nodemailer`.
- [ ] 1.3 Configure `vite.config.ts` and `tsconfig.json` to match `TCS-Paint`.
- [ ] 1.4 Set up `.env` file based on `TCS-Paint` pattern.

## 2. Structure Implementation
- [ ] 2.1 Create standard folders: `src/pages`, `src/custom-components`, `src/assets`, `src/helper`, `src/hooks`.
- [ ] 2.2 Create `NavBar.tsx` and `footer.tsx` adapted from `applience-repair-frontend` style but using `TCS-Paint` code structure.
- [ ] 2.3 Create `App.tsx` with Router setup matching `TCS-Paint`.

## 3. Page Creation
- [ ] 3.1 Create `src/pages/Home.tsx` with a Hero section (Junk Removal theme).
- [ ] 3.2 Create `src/pages/About.tsx`, `src/pages/Gallery.tsx`, `src/pages/Form.tsx` skeletons.

## 4. Styling
- [ ] 4.1 Configure `index.css` with Tailwind directives.
- [ ] 4.2 Apply "Appliance Repair" style (colors, spacing) to the components.

